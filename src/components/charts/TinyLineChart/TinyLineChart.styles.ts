import styled from 'styled-components';
import { ResponsiveContainer } from 'recharts';

const ResponsiveContainerWrapper = styled(ResponsiveContainer)`
  position: absolute;
  top: 60%;
  transform: translateY(-50%);
  right: 2rem;
`;

export {ResponsiveContainerWrapper};
